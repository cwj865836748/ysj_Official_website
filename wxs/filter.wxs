var numSort = function (index, schemeIndex) {
  var n = (parseInt(index + '' + schemeIndex) + 1).toString()
  return n[1] ? n : '0' + n
}
var numLength = function (num) {
  var n = num.toString()
  return n[1] ? n : '0' + n
}
module.exports = {
  numSort: numSort,
  numLength:numLength
};